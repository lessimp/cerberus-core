# Server settings
server.port=8080

# Database settings (Linking to the Nexus VM via Tunnel)
spring.datasource.url=jdbc:mysql://localhost:3306/cerberus?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=SovereignPassword123

# Hibernate auto-configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
